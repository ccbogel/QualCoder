#export DH_VERBOSE=1
export PYBUILD_NAME=qualcoder

%:
    dh $@ --with python3 --buildsystem=pybuild


override_dh_auto_clean:
    dh_auto_clean -O--buildsystem=pybuild
    find . -type d -name '__pycache__' -empty -delete
